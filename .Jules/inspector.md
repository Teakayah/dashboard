## 2025-02-25 - Ignored outdated snippet and tested None edge case
**Learning:** The prompt provided an outdated code snippet for `extract_nhpi` that contradicted the actual codebase. As per memory rules, it was evaluated as purely outdated context without explicit directives to implement it.
**Action:** Safely ignored the snippet, preserved existing `extract_nhpi` logic which already tested the empty list edge case, and implemented an additional edge case test for `None`.

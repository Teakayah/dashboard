## 2026-05-05 - Removed unused code with Ruff
Learning: Using `ruff check .` highlighted numerous unused variables, unused imports, and bare except blocks across various Python files.
Action: Regularly run `ruff check .` to identify dead code, unused imports, and syntax issues. Fix them proactively to keep the codebase lean and clean.

1. **Fix `test_rebuild_analyses.py` by resetting mocks between tests or using fresh mock objects**
   - The test `test_main_success_with_changes` fails intermittently because `REBUILDERS["test.html"]` is a `MagicMock` shared across tests when it's not reset. Wait, the mock in `@patch('deployment.rebuild_analyses.REBUILDERS', {"test.html": MagicMock()})` is instantiated at module parse time. Thus, the exact same `MagicMock` instance is used in every test that is decorated with it, accumulating calls.
   - The fix is to use a test-local fixture or create a new mock inside the test, or just call `mock_rebuild_fn.reset_mock()` before using it, or use a function in `@patch('deployment.rebuild_analyses.REBUILDERS', new_callable=...)`. Let's just fix it by explicitly resetting the mock or creating it fresh per test.
